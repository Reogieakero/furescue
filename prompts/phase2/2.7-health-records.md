# UNIT 2.7 — Health-records: `public/admin/health-records.html` → `public/admin/health-records.php`

You are executing one unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. Unit 2.0 must be ✅ (shared admin-shell include). Units 2.1–2.6 recommended as pattern references.
2. On completion update HANDOFF.md: row 2.7 ✅ + dated ledger entry.

## Global locked rules
- Full PHP SSR; pages call Entities/Repositories/Services in-process — NO fetch for initial render. NEVER edit `src/Controllers|Repositories|Services|Entity|Http`, migrations, tests, seeders, dbtool, bin. No frameworks. No new composer deps. PHP ≥ 8.1.
- EVERY echoed dynamic value through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`. Pixel-identical DOM/classes to current JS output; never rename classes.
- Auth via sessions; set `$requiredRole` from the entry JS `requireAuth([...])` call, then `require __DIR__ . '/../includes/guard.php';`.
- Environment: portable PHP `%TEMP%\opencode\furescue-php\php.exe`; mirror `%TEMP%\opencode\furescue-mirror` (copy changed files before testing); DETACHED server via `Start-Process -ArgumentList "-S","127.0.0.1:<port>","-t","...public","...index.php"` (never Start-Job); NO DB creds exist — verify 200 + DOM markers + `php -l`, data fidelity by code review against controller serialization; harnesses only in mirror.
- Class strings: emit FINAL tailwind-merge-resolved strings. Read every component `.js` before writing its PHP twin.

## Locked SSR architecture
Same pattern as prior units: PHP builds payloads via Repositories mirroring CONTROLLER serialization exactly → renders FULL page identical to post-load JS DOM → embeds `<script>window.__PAGE_STATE__ = <?= json_encode($state, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT) ?>;</script>` → entry JS gets minimal hydration branch (merge embedded state, SKIP initial render + client requireAuth, keep ALL interactivity: charts re-draws, search/filter, flyout open/close, record CRUD modals); legacy fetch flow kept as fallback. Server always paints full page, never skeletons/loading states.

## Work items
1. READ FIRST: `public/admin/js/health-records.js`, everything under `public/admin/js/pages/health-records/` (state + components incl. charts + flyout), old `public/admin/health-records.html` head/scripts (note any chart lib tags). Identify which fetches build the KPIs/charts/table (health records list endpoint + activity feed — find BOTH controller methods and their exact serialization), and what stays pure interactivity.
2. Trace data source(s): replicate queries via HealthRepository in-process for default view state; chart series values are derived client-side from rows — compute identically server-side and render the same DOM the chart lib expects (or keep chart draw JS-only if it mutates canvas/SVG after boot — then SSR only the containers).
3. Write `public/admin/health-records.php`: bootstrap autoload+Dotenv, guard, data builders, full SSR of KPI cards/chart containers/table rows page 1/flyout container markup, `__PAGE_STATE__` embed, same script/style tags as old HTML.
4. Patch `health-records.js` hydration branch per architecture above. Update `app-shell.js` NAV_TARGETS `health-records → /admin/health-records.php`.
5. Copy to mirror; `php -l`; start server; curl anon → expect 302 `/auth/login.php`; harness-authenticated render check if needed.
6. Delete `public/admin/health-records.html` ONLY after verification passes.

## Done when
PHP lints; anon redirect proven; KPI/table markup path verified; hydration avoids double-render; HANDOFF.md updated (state shape + class-string notes + whether charts SSR or hydrate).

## Report format
Status | files created/modified/deleted | state shape summary | chart handling decision + reason | class-string notes | deviations/blockers.
