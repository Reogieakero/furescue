# UNIT 2.8 — Health-record (detail): `public/admin/health-record.html` → `public/admin/health-record.php` + FINAL WRAP

You are executing the FINAL unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. Units 2.0–2.7 must ALL be ✅. Unit 2.7 specifically (this page is health-records' drill-down — reuse its state-shape notes).
2. On completion update HANDOFF.md: row 2.8 ✅ + ledger entry + a "Phase 2 complete" section with the user's DB smoke-test checklist.

## Global locked rules
- Full PHP SSR; pages call Entities/Repositories/Services in-process — NO fetch for initial render. NEVER edit `src/Controllers|Repositories|Services|Entity|Http`, migrations, tests, seeders, dbtool, bin. No frameworks. No new composer deps. PHP ≥ 8.1.
- EVERY echoed dynamic value through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`. Pixel-identical DOM/classes to current JS output; never rename classes.
- Auth via sessions; set `$requiredRole` from the entry JS `requireAuth([...])` call, then `require __DIR__ . '/../includes/guard.php';`.
- Environment: portable PHP `%TEMP%\opencode\furescue-php\php.exe`; mirror `%TEMP%\opencode\furescue-mirror` (copy changed files before testing); DETACHED server via `Start-Process -ArgumentList "-S","127.0.0.1:<port>","-t","...public","...index.php"` (never Start-Job); NO DB creds exist — verify 200/redirects + DOM markers + `php -l`, data fidelity by code review against controller serialization; harnesses only in mirror.
- Class strings: emit FINAL tailwind-merge-resolved strings. Read every component `.js` before writing its PHP twin.

## Locked SSR architecture
Same pattern as prior units: PHP builds the record payload via Repositories mirroring CONTROLLER serialization exactly → renders FULL page identical to post-load JS DOM → embeds `<script>window.__PAGE_STATE__ = <?= json_encode($state, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT) ?>;</script>` → entry JS gets minimal hydration branch (merge embedded state, SKIP initial render + client requireAuth, keep ALL interactivity: tabs, update forms, media, modals); legacy fetch flow kept as fallback.

## Work items
1. READ FIRST: `public/admin/js/health-record.js`, everything under `public/admin/js/pages/health-record/` (state + components incl. page.js), old `public/admin/health-record.html`. Identify how the id is read (`?id=`), which fetches build initial view (record endpoint → its controller method's exact serialization), and what stays pure interactivity.
2. Query-param handling: read `$_GET['id']`; validate UUID shape; missing/malformed/not-found → redirect `/admin/health-records.php` (or mirror whatever the JS does on failure if different).
3. Write `public/admin/health-record.php`: bootstrap autoload+Dotenv, guard, validation, data builders, full SSR, `__PAGE_STATE__` embed, same script/style tags as old HTML.
4. Patch `health-record.js` hydration branch per architecture above.
5. Link flips NOW allowed: `'health-records.html'` back-links inside this page's JS/components → `/admin/health-records.php`; AND flip any forward link from unit 2.7 into this detail page (`.html?id=` → `.php?id=`). Sweep ALL of `public/admin/js/**` for leftover `.html` hrefs/targets pointing at converted pages and fix stragglers.
6. Copy to mirror; `php -l`; start server; curl anon no-id → expect 302 login; harness-authenticated no-id → expect 302 list page.
7. Delete `public/admin/health-record.html` ONLY after verification passes.

## FINAL WRAP (after deletion)
1. Prove zero-`.html` end-state: `Get-ChildItem public -Recurse -Filter *.html` must return NOTHING (uploads assets excluded by nature).
2. Grep `public/admin/js` for remaining `.html` references — each hit must point at a legitimately non-existent page or be fixed.
3. Confirm API untouched: `git status` shows no modifications under `src/Controllers|Http` etc.; `public/index.php` diff limited to Phase 1's known homepage+passthrough branches.
4. Write final HANDOFF.md "Phase 2 complete" section containing the USER smoke-test checklist for once they add their own `.env`: start mirror server, login at `/auth/login.php` with admin@furescue.local / Password123!, visit `/admin/index.php`, `/admin/cases.php`, one case detail, `/admin/animals.php`, `/admin/reports.php`, `/admin/rescuers.php`, `/admin/health-records.php`, one record detail — confirm rows render identically to pre-migration screenshots/git history, then repeat one rescuer-visible page under a rescuer account to prove role guards.
5. Report completion summary.

## Done when
PHP lints; redirects proven; all `.html` gone from `public/`; link sweep clean; HANDOFF.md finalized.

## Report format
Status | files created/modified/deleted | full-page inventory table (.php ↔ former .html) | deviations/blockers | user checklist recap.
