# UNIT 2.1 — Dashboard: `public/admin/index.html` → `public/admin/index.php`

You are executing one unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. Unit 2.0 must be ✅ (admin-shell include exists — USE ITS CONTRACT from the ledger instead of re-deriving). If not, STOP.
2. On completion update HANDOFF.md: row 2.1 ✅ + dated ledger entry.

## Global locked rules
- Full PHP SSR; pages call Entities/Repositories/Services in-process — NO fetch for initial render. NEVER edit `src/Controllers|Repositories|Services|Entity|Http`, migrations, tests, seeders, dbtool, bin. No frameworks. No new composer deps. PHP ≥ 8.1.
- EVERY echoed dynamic value through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`. Pixel-identical DOM/classes to current JS output; never rename classes.
- Auth via sessions; `$requiredRole = 'admin';` then `require __DIR__ . '/../includes/guard.php';`.
- Environment: portable PHP `%TEMP%\opencode\furescue-php\php.exe`; mirror `%TEMP%\opencode\furescue-mirror` (copy changed files there before testing); DETACHED server via `Start-Process -ArgumentList "-S","127.0.0.1:<port>","-t","...public","...index.php"` (never Start-Job); NO DB creds exist — verify 200 + DOM markers + `php -l`, data fidelity by code review against controller serialization; harnesses only in mirror.
- Class strings: emit FINAL tailwind-merge-resolved strings (later same-group utility wins over earlier base; e.g., size lg `text-sm` replaces base `text-[13px]`). Read every component `.js` before writing its PHP twin.

## Locked SSR architecture for this page
1. `public/admin/index.php`: bootstrap `vendor/autoload.php` + Dotenv `createImmutable(dirname(__DIR__, 2))->safeLoad()`, set `$requiredRole='admin'`, require guard, build ALL payloads below via Repositories mirroring the CONTROLLER serialization EXACTLY (JS renders off API field names; entity `toArray()` may differ — trust controller output keys), render the FULL page identical to the JS post-load DOM (`loading=false` equivalent), embed `<script>window.__PAGE_STATE__ = <?= json_encode($state, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT) ?>;</script>` before the module script tag.
2. Edit `public/admin/js/dashboard.js` MINIMALLY: when `window.__PAGE_STATE__` exists → merge it into the state module and SKIP initial render()/innerHTML build AND the client requireAuth redirect (server already guarded), but STILL run every init function (lucide icons, initShell, dropdowns, carousel drag, queue tabs/pagination/actions, Leaflet map, activity pagination, date filler). Without `__PAGE_STATE__`, keep the legacy 12-fetch flow as fallback. Do NOT edit component submodule files except where an export must be added or hrefs change.
3. Server-side we ALWAYS paint the full page — never render skeletons (accepted deviation from no-cache JS behavior).

## Recon already done (trust, but spot-check)
Entry: `public/admin/js/dashboard.js` (`requireAuth(['admin'])`). Tree = AppShell{Sidebar, Topbar, main: [Greeting, KpiGrid, AttentionRow(AttentionQueue+HealthCarousel+RescuersCard), MapCard, cols[ChartCard,ElearningCard], cols[ActivityTable,AuditLogCard]]}. Components live in `public/admin/js/pages/dashboard/components/`; state shape in `pages/dashboard/state.js`; interactivity in `pages/dashboard/queue.js` + `map.js` + `carousel.js`.

Data sources (see `public/admin/js/lib/admin-data.js`) → replicate each payload server-side by reading the corresponding controller method FIRST:
- `/analytics/overview` → `.stats` (AnalyticsController::overview)
- `/analytics/adoption-trends` → `.trends` `[{day, completed}]`
- `/reports?status=pending_verification` and `/reports` (ReportController::index — note exact item fields)
- `/users?role=rescuer&account_status=pending` and `=active` (UserController::index)
- `/adoptions?status=pending` (AdoptionController::index)
- `/cases` (CaseController::index — find where `assigned_rescuer_name` join happens)
- `/notifications?is_read=false` (NotificationController)
- `/elearning/modules?published_status=published|draft` (ElearningController)
- `/reports/map/heatmap` → `.points`
- `/health/updates` → `.updates` (HealthController)

Helpers to reimplement identically in PHP (source: `pages/dashboard/components/helpers.js`, `util.js`): `shortId` ("#"+first4 of uuid minus dashes, uppercased), `timeAgo` (same-day→HH:MM locale en-US 2-digit / "Yesterday" / "<N> days ago" / "Mon D"), `titleCase` (snake_case→Title Case), `truncate(text,28)` slice(0,n-1)+"…", `initials` (2 words uppercase), `buildWeekChart(trends)` (7 bars Mon–Sun, bar height `Math.round(count/max*100)`, peak gets coral class, growth=null unless prev-week sum>0 then rounded % delta).

Constants: queue pagination `QUEUE_PAGE_SIZE=7`, activity `ACTIVITY_PAGE_SIZE=5` — server renders page 1 only; JS handles rest. PaginationBar markup — READ `public/js/components/ui/pagination.js` for classes. MapCard Select — READ `public/js/components/ui/select.js`.

Sidebar badges: JS merge is `{notifications:3, ...getNavBadges(), ...badges}` with `badges={reports: reportsTotal, health: healthUpdates.total, applications: pendingAdoptions.total}`; localStorage empty server-side ⇒ pass exactly that map into admin-shell's `$navBadges`.

Head/asset nuances: `#admin-date` stays EMPTY (JS fills). Preserve Leaflet tags exactly as old `index.html` had them — leaflet CSS via site-head's `$pageCss`, the two Leaflet `<script>` tags hand-placed before the module script. Fonts: use site-head's new `$fontsHref` override (unit 2.0) with the admin Google-Fonts URL from the old HTML head.

Map card: `#case-density-map` div; `#heat-count` initial text "0 Active pins · Live"; after hydration `initCaseDensityMap(state.heatmap)` receives points — confirm exact shape in `pages/dashboard/map.js` before building the PHP payload key.

## Work order
1. Read ALL files listed above plus old `public/admin/index.html` head/scripts block.
2. Write `public/admin/index.php` (data builders → state assembly → component-by-component HTML echo).
3. Patch `dashboard.js` hydration branch.
4. Copy both (+ any edited js) to mirror; `php -l`; start server; curl anon hit → expect 302 Location `/auth/login.php` (guard proof). For an authenticated render test WITHOUT DB, use a mirror-only harness that fakes session + stubs repository inputs if repositories can't run without PDO — never commit harness code.
5. Delete `public/admin/index.html` ONLY after verification passes.
6. Update HANDOFF.md (row 2.1 ✅, ledger: files, state-shape contract embedded as JSON, class-string notes for KPI cards/queues/chart bars, deviations).

## Report format
Status | files created/modified/deleted | __PAGE_STATE__ shape summary | tricky class-string computations | deviations/blockers | user's later DB smoke-test note (URLs + login admin@furescue.local / Password123!).
