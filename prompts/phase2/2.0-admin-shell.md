# UNIT 2.0 — Admin Shell → Shared PHP Include

You are executing one unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. If unit 2.0 is marked done/✅, STOP — report and ask.
2. On completion you MUST update that file: status table row 2.0 → ✅, append dated ledger entry with files touched + key notes.

## Goal of THIS unit
Port the shared admin chrome (`AppShell` = Sidebar + Topbar + main slot) into ONE reusable PHP include that every later admin page will use. Create NO admin page yet. Do NOT delete any `.html` yet.

## Global locked rules (apply everywhere)
- Full PHP SSR; zero `.html` under `public/` at project end. Pages call Entities/Repositories/Services in-process — NO fetch for initial render.
- NEVER edit: `src/Controllers`, `src/Repositories`, `src/Services`, `src/Entity`, `src/Http`, migrations, tests, seeders, dbtool, bin. No frameworks/templating engines. No new composer deps. PHP ≥ 8.1 syntax.
- EVERY echoed dynamic value passes through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`.
- Output must be PIXEL-IDENTICAL to current JS-rendered DOM: same CSS files, same class names, same element structure. Never rename classes.
- Auth = PHP sessions (`$_SESSION['user']` = id/email/role). `public/includes/guard.php`: define `$requiredRole` BEFORE including it; anon → 302 `/auth/login.php`; wrong role → 302 `/index.php`.

## Environment facts (verified — trust these)
- Portable PHP CLI: `%TEMP%\opencode\furescue-php\php.exe` (8.3.33).
- Repo root has NO vendor/ and OneDrive blocks composer there. Verification MIRROR with vendor installed: `%TEMP%\opencode\furescue-mirror`.
- Test loop: copy changed repo files into mirror (same relative paths) → start DETACHED server:
  `Start-Process -FilePath "$env:TEMP\opencode\furescue-php\php.exe" -ArgumentList "-S","127.0.0.1:<port>","-t","`"$env:TEMP\opencode\furescue-mirror\public`"","`"$env:TEMP\opencode\furescue-mirror\public\index.php`"" -PassThru -WindowStyle Hidden`
  (Start-Process, NEVER Start-Job — jobs die with the shell.) Then `curl.exe http://127.0.0.1:<port>/...`, kill the process after.
- NO working DB credentials exist (user wires `.env` later). Verify HTTP status + DOM markers + `php -l` only. Any DB-touching test harness lives ONLY in the mirror, never the repo.
- PHP ≥8 built-in server routes EVERY request through the router script `public/index.php`; real existing files are served/executed natively via its guarded `return false` passthrough.

## Class-string mechanics (critical)
Old UI was built by ES modules using `cn()` = tailwind-merge(clsx(...)). Your PHP must emit FINAL merged strings. Rules: cva output = base + variant + size joined by spaces; twMerge drops EARLIER conflicting utilities when a LATER one conflicts in the same group (Button size lg adds `text-sm` which REPLACES base `text-[13px]`; size sm has no font-size so `text-[13px]` survives). Custom non-Tailwind classes (e.g., `admin-shell`, `sidebar-*`) never conflict, appended last. ALWAYS read the component `.js` before writing its PHP twin.

## Work items
1. READ first: `public/admin/js/layout/app-shell.js`, `sidebar.js`, `topbar.js`; `public/js/lib/swr.js` (`getNavBadges` semantics); `public/js/components/ui/dropdown-menu.js`; `public/includes/site-head.php`; `public/includes/guard.php`.
2. Extend `public/includes/site-head.php` MINIMALLY and BACKWARD-COMPATIBLY: accept an optional `$fontsHref` override — landing uses the existing Nunito Google-Fonts link (default unchanged), while admin pages load Fraunces + Nunito + IBM Plex Mono (copy the exact fonts URL from `public/admin/index.html` `<head>`). Same pattern as existing `$pageCss` support.
3. Create `public/includes/admin-shell.php`, parameterized via variables defined before include: `$adminUser` (array id/full_name/email/role/profile_photo_url), `$activeNav` (string key), `$navBadges` (assoc map, may be empty), `$adminChildren` (pre-rendered HTML string for `<main>` contents). It renders the COMPLETE `.admin-shell` DOM identical to `AppShell()`: brand block, NAV_GROUPS sections with `data-nav` anchor items (`href="#"`, icons via `<i data-lucide="..." class="icon"></i>`, badge spans hidden when falsy), active-section highlighting matching `setActiveSection` logic, topbar with EMPTY date span (`id="admin-date"` — JS fills it), notification bell dropdown (badge merge semantics server-side = `['notifications'=>3, ...$navBadges]` — localStorage is empty server-side so `getNavBadges()` contributes nothing), user menu dropdown with profile rows + sign-out item (markup only; behavior stays in JS), then echoes `$adminChildren` inside `<main>`. Match attribute order and indentation style of the JS templates.
4. In `public/admin/js/layout/app-shell.js` update `NAV_TARGETS` ONLY for the dashboard key → `/admin/index.php`. Leave every other target `.html` — later units flip their own.
5. Verify: `php -l` both PHP files. Build a HARNESS page inside the MIRROR ONLY (e.g., `mirror/public/admin/__shelltest.php`) that fakes `$_SESSION['user']`, sets the params, includes guard + site-head + admin-shell with trivial `$adminChildren`, curl it: expect 200 + presence of `.admin-shell`, `#admin-date`, sidebar nav items, dropdown markup. Confirm landing `/` still renders unchanged (site-head regression check). Delete harness after.

## Done when
Both PHP files lint clean; harness curl shows expected markers; landing unaffected; `app-shell.js` dashboard target flipped; HANDOFF.md updated (row 2.0 ✅ + ledger entry incl. exact param names/contract of `admin-shell.php` so later units don't re-read everything).

## Report format (return in your final message)
Status ✅/⚠️ | files created/modified | admin-shell.php parameter contract | class-string notes | deviations/blockers | leftover risks.
