# PHASE 2 HANDOFF — HTML→PHP Migration (FurEscue)

> LIVING FILE. Every agent finishing a unit MUST update this file before ending its session.
> Every agent starting a unit MUST read this file first. If your unit's prerequisites are not ✅, STOP and report — do not improvise past gaps.

## How to use these prompts
Units run strictly in order: 2.0 → 2.1 → 2.2 → 2.3 → 2.4 → 2.5 → 2.6 → 2.7 → 2.8.
Drag exactly ONE `2.x-*.md` into a fresh agent session. The prompt is fully self-contained.

## Unit status

| Unit | Page | Status | Notes |
|------|------|--------|-------|
| 2.0 | Admin shell (shared include) | ✅ done | `public/includes/admin-shell.php` — contract in ledger below |
| 2.1 | Dashboard `admin/index.php` (was `.html`) | ✅ done | migrated onto shared include; legacy shell deleted; see ledger |
| 2.2 | Cases `admin/cases.html` | ✅ done | migrated to `public/admin/cases.php`; see ledger |
| 2.3 | Case-detail `admin/case-detail.html` | done | migrated to `public/admin/case-detail.php`; see ledger |
| 2.4 | Animals `admin/animals.html` | ✅ done | migrated to `public/admin/animals.php`; see ledger |
| 2.5 | Reports `admin/reports.html` | ✅ done | migrated onto shared include; legacy shell deleted; see ledger |
| 2.6 | Rescuers `admin/rescuers.html` | ✅ done | migrated onto shared include; legacy shell deleted; see ledger |
| 2.7 | Health-records `admin/health-records.html` | ✅ done | migrated to `public/admin/health-records.php`; see ledger |
| 2.8 | Health-record `admin/health-record.html` | ✅ done | migrated to `public/admin/health-record.php`; see ledger |

## Completed-so-far ledger (append per unit)
*(format: `- [2.x] <date> — files touched, key class-string notes, deviations`)*

- [Phase 1, prior session] DONE + verified: `public/includes/{site-head,header,footer,guard,homepage}.php`, `src/Auth/SessionAuth.php`, `public/auth/login.php` (+`auth/js/auth.js` interactivity-only rewrite), landing ported into `public/index.php` @ `/`; deleted `public/index.html`, `public/landing/index.html`, `public/auth/login.html`. Deviations accepted: static-passthrough (`return false`) added in `public/index.php` (PHP ≥8.3 built-in server routes EVERY request through the router script — without it assets never serve); navbar "Get Started" → `/auth/login.php` (signup page never existed); icons still hydrate client-side via lucide.
- [2.0, 2026-08-22] DONE + verified: created `public/includes/admin-shell.php`; extended `public/includes/site-head.php` with optional `$fontsHref` (default Nunito link unchanged when unset); flipped `NAV_TARGETS.dashboard` → `/admin/index.php` in `public/admin/js/layout/app-shell.js` (all other targets still `.html`). Verified via mirror harness: HTTP 200 + `.admin-shell`/`#admin-date`/nav items/active-highlight/dropdown markup; badge override (reports=21) + default notifications=3 + static badges intact; anon guard 302 → `/auth/login.php`; landing `/` regression-clean (Nunito link, no Fraunces). Both files `php -l` clean.
  - **`admin-shell.php` contract** — define BEFORE `require __DIR__ . '/../includes/admin-shell.php';`: `$adminUser` = array id/full_name/email/role/profile_photo_url (photo empty → falls back to pravatar img like JS); `$activeNav` = string lowercase key ('' → 'dashboard'); `$navBadges` = assoc map badgeKey→value, may be empty (server merge = `['notifications'=>3] + $navBadges`; explicit `null` value suppresses that item's static badge, matching JS); `$adminChildren` = pre-rendered HTML string echoed inside `<main class="admin-main">`. Pair with site-head: set `$fontsHref` to Fraunces+Nunito+IBM Plex Mono URL and `$pageCss=['/admin/css/admin.css', ...]`.
  - **DISCOVERED DISCREPANCY (pre-existing, committed in d687357 as "early admin PHP scaffolding", never ledgered):** unit 2.1's source page `public/admin/index.html` NO LONGER EXISTS — a full dashboard port already lives at `public/admin/index.php`, built on a LEGACY function-API shell: `public/admin/includes/{shell.php,ui-helpers.php}` exposing `admin_app_shell($children, $badges, $notifications, $activeNav)` (different file + different API than this unit's variable-based include). Dashboard nav target was already relative `index.php` before this unit made it absolute. Unit 2.1 must be re-scoped as reconcile/migrate `index.php` onto `public/includes/admin-shell.php` + decide fate of legacy `admin/includes/shell.php` (left untouched here; both coexist harmlessly). Also note: existing `index.php` currently passes the admin fonts URL through `$pageCss` instead of `$fontsHref`.
  - No deviations from unit spec; no blockers left open.
- [2.1, 2026-08-22] DONE + verified (re-scoped per 2.0 ledger — page was already ported): migrated `public/admin/index.php` off the legacy function-API shell onto `public/includes/admin-shell.php`: removed `require includes/shell.php`, replaced `admin_app_shell()` call with `$adminUser/$activeNav/$navBadges/$adminChildren` setup + `ob_start()`/`require admin-shell.php` capture into `$pageHtml`; fonts moved from `$pageCss` to new `$fontsHref`; `$pageCss` now `['/admin/css/admin.css', leaflet css]`. DELETED legacy `public/admin/includes/shell.php` (sole consumer was index.php; `includes/ui-helpers.php` KEPT — still supplies e()/table/pagination/avatar/select helpers). `dashboard.js` hydration branch (`window.__PAGE_STATE__` → Object.assign + skip requireAuth + initPageInteractions) already existed — untouched. Verified: `php -l` clean; anon `/admin/index.php` → 302 `/auth/login.php` (guard precedes DB connect) incl. after shell deletion; mirror harness (canned data, no DB) → 200 with `.admin-shell`, active-highlight dashboard, badge overrides 42/7/9, Fraunces+IBM-Plex-Mono override link, leaflet+admin.css present, default Nunito absent.
  - **`window.__PAGE_STATE__` shape (index.php `$state` keys = JS state module keys exactly):** `{overview{reports,reports_verified,cases,cases_resolved,animals,animals_adopted,adoptions_pending,adoptions_completed,rescuers_on_duty,residents}, reports[], reportsTotal, reportsPending{items,total}, rescuersPending{items,total}, healthUpdates{items,total}, adoptionsPending{items,total}, rescuers[](+duty_status), activity[](case rows w/ animal_description,address_text,assigned_rescuer_name,status), chart[{day,count,h,coral}], growth(int|null), elearning{published,drafts,items}, notifications{items,total}, heatmap[], decisionCount, activityPage}`. Chart bars use `h` (0-100 int) + `coral` bool — matches helpers.js buildWeekChart consumers.
  - Deviations accepted: sidebar notifications badge now static default **3** (unit-spec contract `['notifications'=>3]+$navBadges`) instead of legacy real unread total — spec-mandated; `$fontsHref` renders `&amp;`-escaped ampersands via htmlspecialchars (parses to identical DOM); admin avatar uses session/profile photo when non-empty else pravatar fallback; full authed-page HTTP test impossible without DB creds — data fidelity code-reviewed against controller serialization (state keys match JS module 1:1).
  - User DB smoke-test note: wire `.env` (MySQL80 local), login `admin@furescue.local` / `Password123!`, hit `/admin/index.php`.
- [2.5, 2026-08-23] DONE + verified: created `public/admin/reports.php` (full SSR of default view + `__PAGE_STATE__` embed); patched `public/admin/js/reports.js` with hydration branch (`window.__PAGE_STATE__` → Object.assign into state module, skip requireAuth/initial fetch/render via `app.childElementCount` guard, keep ALL interactivity: filter chips re-filter cached rows client-side, search/sort/pagination re-render table, drawer open/close + verify/dismiss/assign/timeline actions; legacy fetch flow kept as fallback); flipped `NAV_TARGETS.reports` → `/admin/reports.php` in `app-shell.js`. DELETED `public/admin/reports.html` (+ mirror copy). Verified: `php -l` clean; anon `/admin/reports.php` → 302 `/auth/login.php`; mirror harness (sqlite stub of `App\Database`, session-seeded, 17 reports/4 cases covering pending+verify-dismiss/open-assigned-in_progress-resolved cases/dismissed) → 38/38 DOM assertions PASS incl. sort fidelity.
  - **Default view = what PHP pre-renders:** state.js defaults `{filter:'all', query:'', sort:'assigned', page:1}`; PAGE_SIZE 15; pagination bar only when list > 15. Filter chips/search/sort NEVER refetch — they refilter/sort cached rows client-side, so only the default view is server-painted. KPI counts derive from ROWS not the overview endpoint (`reportCounts()`: status tallies + cases ≠/== resolved); overview stats still embedded in state for parity. Drawer appends to `<body>` at runtime → NO container markup needed in SSR.
  - **`window.__PAGE_STATE__` shape (reports):** `{overview{...10 AnalyticsController stats}, reports[Report::toArray() ×100 created_at DESC], cases[c.*+animal_description+address_text+assigned_rescuer_name ×100 updated_at DESC], rescuers[u.*-password_hash+duty_status ×100]}`. Keys match state.js module exactly (filter/sort/page/query intentionally NOT embedded — JS defaults already correct).
  - **Class-string notes:** table-action buttons replicate cva base INCLUDING `disabled:pointer-events-none disabled:opacity-50` which shared `ui-helpers.php` BTN_BASE lacks — local `$rptButton` closure emits final twMerge-resolved strings (`base + variant + ' h-7 px-3'`; Export CSV uses `' h-8 px-4'` outline). Sort select = `select_control('report-sort', …, className:'report-sort-control')` — byte-identical to JS `Select`. Pagination = `pagination_bar(n,15,1)` matches `PaginationBar` incl. Set-based page items + ellipsis. Sidebar badges: `$navBadges = ['notifications'=>null,'reports'=>count(reports)]` — null SUPPRESSES the static notifications badge because ReportsPage passes `notifications:0` in JS (badge falsy → absent); reports badge = row count.
  - **Sort fidelity ("assigned"):** PHP `usort` mirrors `sortKey`: `[case&&assigned_rescuer_id?0:1, caseStatusRank(null0/open1/assigned2/in_progress3/resolved4), -strtotime(created_at)]` — stable in PHP≥8 like JS. Consequence: rank-1 rows (verified case w/o rescuer) sort BELOW all no-case rows and land on page 2 — proven by harness ('Assign rescuer' button correctly absent from SSR page 1).
  - **Controller quirk preserved:** `UserController::indexRescuers` ignores the `role=rescuer` param (only filters account_status=active) → PHP data builder replicates verbatim (all active users w/ duty_status), so `state.rescuers` may contain non-rescuers exactly as the API returns today; consumers self-filter.
  - Harness trick (mirror-only, deleted after): portable php.ini lacks pdo_sqlite but DLL ships in ext/ — launched detached server with `-d extension=php_pdo_sqlite.dll -d extension=php_sqlite3.dll` and stubbed `App\Database::connect()` to seeded sqlite :memory: (stub class defined BEFORE vendor/autoload so composer never loads the real one). NOTE: detached servers die between shell-tool invocations — run Start-Process + curl in ONE command.
  - No deviations from unit spec; no blockers left open.
- [2.6, 2026-08-23] DONE + verified: created `public/admin/rescuers.php` on `admin-shell.php` + `site-head.php` (`$fontsHref` Fraunces/Nunito/IBM-Plex-Mono, `$pageCss=['/admin/css/admin.css']`, no leaflet). Data: three queries replicating `UserController::indexRescuers` SQL verbatim (active/suspended/pending; `u.*` minus password_hash + `COALESCE(d.status,'off_duty') AS duty_status`, ORDER created_at DESC LIMIT 100) + all reports via `ReportRepository->paginate(1,100,[])->toArray()`. SSR children: PageHead(Export CSV outline+download) → `#rescuer-kpis` (5 tiles, counts = rescuerCounts() replica) → `.rescuer-split` with panel (`#rescuer-filters` q-tabs All/Active/On duty/Off duty/Pending + `#rescuer-search`) and ActiveTable (10 rows/page, `pagination_bar` when >10, duty stamps accent/muted, Suspend destructive-sm vs Activate outline-sm w/ data-action/data-id) → `#rescuer-detail` empty-state (fresh load never has selection). Deleted `public/admin/rescuers.html`.
  - **`window.__PAGE_STATE__` shape (rescuers state module keys):** `{rescuers:[active...suspended], pending[], reports[], caseActivity:{} (stdClass), filter:'all', query:'', page:1}`. `selectedId/selectedRescuer/selectedRescuerCases` intentionally OMITTED (JSON can't express `selectedRescuer: undefined`; server never has a selection) — hydration branch restores them from localStorage via existing `hydrateSelection()`+`restoreSelection()` and toggles row `is-selected`.
  - `rescuers.js` hydration branch: `Object.assign(state, __PAGE_STATE__)` → `setNavBadge("rescuers", pending.length)` → empty-#app fallback re-render (getSessionUser) → hydrateSelection + row highlight → createIcons/initShell/initDropdownMenu/initRescuerEvents → restoreSelection. Legacy fetch flow kept as fallback. `app-shell.js` NAV_TARGETS `rescuers` → `/admin/rescuers.php`.
  - Tricky class strings: destructive Button variant not in ui-helpers — final string emitted locally as BTN_BASE + `bg-destructive text-destructive-foreground shadow-sm hover:bg-destructive/90` + `h-7 px-3` (verified = cva/twMerge output; no conflicting groups with base). Export CSV outline-default = base + outline classes + `h-8 px-4`. Badge parity: `$navBadges=['rescuers'=>$pendingCount,'notifications'=>null]` — JS page passes `notifications:0` so the static bell badge is SUPPRESSED server-side via null (fresh-load parity; swr-cached notification counts are unknowable to PHP).
  - Deviations accepted: fixed two now-dead `rescuers.html` links outside unit scope (index.php "View all" + dashboard cards.js twin → `/admin/rescuers.php`); `#admin-date` left EMPTY like legacy rescuers.js (it never called initDate); timeAgo/locale rendering uses shared ui-helpers approximations (accepted since 2.1); filter/query/page localStorage persistence not restored into painted table (server paints fresh defaults; selection IS restored — documented in state shape above); authed render verified via mirror-only harness `public/harness-rescuers.php` (FakePdo shim class App\Database pre-autoload, canned 12 active/1 suspended/2 pending users + 2 reports; asserts KPIs 15/12/3/2/1, tab counts, 10 rows + pagination, badge 2, notifications badge absent, valid state JSON, no password_hash leak). Anon `/admin/rescuers.php` → 302 `/auth/login.php`; all files `php -l` clean; node --check clean on touched JS.
- [2.4, 2026-08-23] DONE + verified (ran while 2.2 in flight in a parallel session — only hard prereq 2.0 ✅ per unit spec): created `public/admin/animals.php` (bootstrap autoload+Dotenv → guard `$requiredRole='admin'` → ui-helpers → data builders → SSR via `public/includes/admin-shell.php` capture + site-head, mirroring index.php structure). Data builders mirror controller serialization exactly: animals rows = AnimalController::index SQL verbatim (`WHERE deleted_at IS NULL ORDER BY created_at DESC LIMIT 100 OFFSET 0`, raw rows incl. JSON-string `photo_urls`); medical flag set = simple `SELECT DISTINCT animal_id FROM animal_medical_records` (provably equal to HealthController::records' `hasMedicalRecord` CASE filter — the only field the page consumes). PHP `normalize()` ports state.js normalize() 1:1. DELETED `public/admin/animals.html`. PATCHED `js/animals.js`: hydration branch (`__PAGE_STATE__` → Object.assign into state module, skip client requireAuth, skip initial render when server painted, bind createIcons/initShell/initDropdownMenu/initAnimalsEvents/initDate, then `restoreSelectedId()` + conditional renderSelection/renderDetail for localStorage-restored selection — no full re-render, no double paint); legacy fetch flow intact as fallback. PATCHED `app-shell.js` NAV_TARGETS `animals → /admin/animals.php` (was the ONLY `animals.html` reference repo-wide). Verified: `php -l` clean; anon `/admin/animals.php` → 302 `/auth/login.php` (guard precedes DB connect); `/admin/index.php` regression-clean 302; mirror harness (stub PDO canned rows, session-seeded admin, generated+deleted in temp) → 200 with all 27 DOM assertions passing: shell/active-nav-highlight animals/page-head stamps/buttons (Add animal default+plus+data-act, Export CSV outline final tailwind-merge string), 4 cards exercising every branch (img vs initial+icon dog/cat, green Medical ×2 / red No records ×2 ribbons, status tones accent/muted×2/jungle), filter tabs counts All·4/Available·1/Pending·1/Adopted·1/Not listed·1 with is-active on all, total-badge 4, empty search value="", hidden selected-store, mini-stats 4/1/1/1, detail empty state, module script tag.
  - **`window.__PAGE_STATE__` shape (keys = state.js state module keys exactly):** `{animals:[{id,name,species("Cat"|"Dog"),breed(ucfirst breed_type|"—"),age(age_estimate|"—"),sex("F"|"M"|"—"),status(label map,"Not listed" default),barangay(color_markings||source-derived "Resident listing"/"Rescued case"|"—"),intake(created_at.slice 10),photo(first of photo_urls JSON array|null),hasMedical(bool)}], query:"", filter:"all", selectedId:null}`. Server always renders selectedId=null; localStorage restore happens in hydration only.
  - Class-string notes: card = `animal-card[ is-selected]` > `.animal-thumb--dog|--cat` > img.animal-thumb-img OR span.animal-thumb-initial(ucfirst name char) + i[data-lucide=cat|paw-print].animal-thumb-icon > ribbon `animal-card-ribbon animal-card-ribbon--green`(Medical)/`--red`(No records); body stamp tone map Available→stamp--accent, Pending/Not listed→stamp--muted, Adopted→stamp--jungle. Animals sidebar nav item has NO badgeKey → no sidebar badge ever renders (rerenderAll's badge lookup no-ops) — `$navBadges=[]` correct. Page paginates nowhere (client-side list only) — no pagination markup needed.
  - No deviations from unit spec; no blockers left open.
- [2.2, 2026-08-23] DONE + verified: created `public/admin/cases.php` (bootstrap mirrors index.php: autoload+Dotenv → guard `$requiredRole='admin'` → ui-helpers → `Database::connect()` → data builders → SSR into `$adminChildren` via admin-shell capture + site-head with `$fontsHref`). Data builders mirror controller serialization exactly: cases rows = CaseController::index SQL verbatim (JOIN reports for animal_description/address_text, LEFT JOIN users for assigned_rescuer_name, `ORDER BY c.created_at DESC LIMIT 100 OFFSET 0`); rescuers = UserController::indexRescuers verbatim (active users LEFT JOIN rescuer_duty_status → duty_status); enrichment = twin of controller closure (reports lookup by report_id, rescuers by assigned_rescuer_id); heatmap = `(new GeoService())->heatmapPoints(null)`. Counts (all/open/assigned/in_progress/resolved) from enriched rows. NOTE: final cases.status ENUM **includes 'open'** — migration `2024_01_01_000006_case_open_status.sql` added it; any earlier recon note saying otherwise is stale. DELETED `public/admin/cases.html`. PATCHED `js/cases.js`: hydration branch (`__PAGE_STATE__` → Object.assign + skip requireAuth/initial fetch; `loadFilterPref()` runs first and if localStorage pref ≠ server default 'in_progress' the tabs re-toggle and `renderCaseList()` re-renders; initCasesEvents/initCaseSort/initCaseMapMode/initDate/renderCaseMap/renderStatusBreakdown always run; `getSessionUser` import added). PATCHED `app-shell.js` NAV_TARGETS `cases → /admin/cases.php`. EXTENDED `public/includes/site-head.php`: optional `$importMapExtras` (assoc map merged into importmap after lucide, `JSON_UNESCAPED_SLASHES`) — used by cases.php to inject chart.js@4.4.4/auto for the donut. FIXED `ui-helpers.php` `time_ago()`: `'g:i A'` → `'h:i A'` (padded-hour parity with JS `toLocaleTimeString` 2-digit). `$navBadges=['notifications'=>0,'cases'=>$allCount]` — CasesPage passes notifications:0 so bell badge hides on this page (matches JS); sidebar cases badge = total count. Verified: `php -l` clean on all touched files; anon `/admin/cases.php` → 302 `/auth/login.php` (guard precedes DB); landing regression 200; mirror harness (stubbed `App\Database` + FakePdo extends PDO routing canned rows by SQL sniff, session-seeded admin; drove the REAL cases.php incl. real repositories) → 200 with all markers passing: shell/admin-date/active-nav cases, KPI grid + dark tile, donut canvas + donut-total 10, tabs "In Progress · 7" active, case-total-badge 10, case-grid with case-card--live + action-text, queue-pagination (7 > PAGE_SIZE 6), map panel + pin count, chart.js importmap extra unescaped, Fraunces/IBM-Plex-Mono fonts, leaflet css+js, `__PAGE_STATE__`, js/cases.js script tag.
  - **`window.__PAGE_STATE__` shape (keys = state.js state module keys exactly):** `{cases:[enriched rows], reports:[report toArray], rescuers:[{...user, duty_status}], heatmap:[], filter:"in_progress", query:"", sort:"createdAt", page:1}`. Enriched case row = DB row + `animal_description`, `address_text`, `assigned_rescuer_name`; report row uses ReportRepository::toArray shape (camelCase lat/lng floats via (float), photoUrls array).
  - Class-string notes: card `article.case-card[ case-card--live when in_progress]`; action area per status — open/unassigned → `button.btn.btn--primary[data-action=assign][data-id][data-name]` + action-text "Waiting for a rescuer...", open/assigned → outline Assign + "Waiting for rescuer to accept the assigned rescue", in_progress → action-text "In progress"; tabs carry data-filter + counts (`In Progress &middot; N`); toolbar `#case-search` input value always empty server-side; map select `#case-map-intensity` default medium, pins toggle starts `.is-active`.
  - No deviations from unit spec; no blockers left open.

- [2.3, 2026-08-23] DONE + verified (ran once 2.2 landed in a parallel session; consumes its cases.php drill-down): created `public/admin/case-detail.php` (bootstrap autoload+Dotenv -> guard `$requiredRole='admin'` -> ui-helpers -> UUID-shape validation of `$_GET['id']` -> `Database::connect()` -> data builders -> SSR children into `$adminChildren` via admin-shell capture + site-head with `$fontsHref` Fraunces/Nunito/IBM-Plex-Mono, `$pageCss=['/admin/css/admin.css', leaflet@1.9.4 dist/leaflet.css]`, `$importMapExtras=['chart.js'=>'https://esm.sh/chart.js@4.4.4/auto']`, leaflet.js `<script>` before module tag). Data mirrors controller serialization exactly: case = `CaseRepository->find($id)->toArray()`; activity = `Repository('case_activity_log',[id,case_id,actor_id,actor_role,action,notes,created_at])->all(['case_id'=>$id],'created_at','ASC')` == CaseController::activity; report = `ReportRepository->find()->toArray()` when report_id set; rescuer = `UserRepository->find()->toArray()` (no password_hash). DELETED `public/admin/case-detail.html`.
  PATCHED js: `case-detail.js` hydration branch (`__PAGE_STATE__` -> Object.assign(state), server-painted guard via `app.childElementCount`, then createIcons/initShell/initDropdownMenu/initCaseDetailEvents; legacy fetch flow kept verbatim as fallback incl. alerts/not-found); back-link flips (`renderNotFound` in case-detail.js + `renderHeader` in pages/case-detail/components/actions.js -> `/admin/cases.php`); forward-link flips left by unit 2.2 (pages/cases/workflow.js card click + pages/cases/components/map.js pin click -> `/admin/case-detail.php?id=` encodeURIComponent'd).
  - **`window.__PAGE_STATE__` shape (keys = pages/case-detail/state.js module keys exactly):** `{caseId, caseData:{...case toArray, report, rescuer, rescuer_name, rescuer_photo}, report|null, rescuer|null, activity:[rows], attachments:[urls], proof:[urls], error:null}`. `photos()` ported 1:1 (JSON string -> array; items string or `{url}`; junk dropped).
  - Class-string notes: workflow panel = panel-head(git-branch, 'Workflow & transactions', stamp--muted '<N> events') + ul.cd-timeline; base event 'Case opened'/'Case created from a verified report.'; type=action||type col; WORKFLOW_LABELS assigned->'Rescuer assigned'/status_change->'Status updated', else title_case(type||'event'); assigned events counted ('Rescuer reassigned' on 2nd+, note Waiting-for-accept when case still assigned); status_change note via `/^Status set to (.+)$/` + STATUS_NOTES map else 'Status changed to '+title_case; actor badges = Badge cva FINAL strings (secondary `border-transparent bg-secondary text-secondary-foreground`, outline `text-foreground`; icon `i.badge-icon` BEFORE text; Badge does NOT escape text -> PHP e() applied everywhere); TimelineItem li.cd-tl-item.cd-tl--<type> > span.cd-tl-dot(index) + .cd-tl-title/.cd-tl-notes?/.cd-tl-meta(span.cd-tl-actor? + span.cd-tl-time > .cd-tl-date?/.cd-tl-clock?); info/source panels use `.dialog-info > .dialog-info-row > .dialog-info-label/.dialog-info-value`; status cell stamp--sm accent(in_progress|resolved)|coral; galleries `div.cd-files > a.cd-file[target=_blank][rel=noopener] > img[alt='Case attachment'|'Rescue proof'][loading=lazy]`; attachment count chip stamp--muted only when >0; proof head icon camera + `.cd-rescuer-meta` badge when proof exists; NO proof add-form (JS gates on role==='rescuer', server always admin).
  - Deviations accepted: missing/malformed/unknown id -> 302 `/admin/cases.php` (validation placed BEFORE DB connect) instead of legacy alert('No case specified.')/not-found panel (spec work-item 2; JS alert path retained in legacy fallback branch only); workflow event dates rendered en-US server-side (`date('M j, Y')` / `date('g:i A')` == en-US toLocale* with same options) - locale-dependent rendering accepted since 2.1; disabled 'Resolved' button intentionally emitted WITHOUT a disabled attribute (ui Button ignores unknown props so legacy DOM never had one; parity kept - button is inert because it lacks data-cd-action); `$navBadges=['notifications'=>0,'cases'=>0]` (legacy page rendered no sidebar badges); `$adminUser.profile_photo_url=''` -> pravatar fallback matching hardcoded legacy Topbar.
  - Verified: `php -l` clean; `node --check` clean on all 4 touched JS files; zero `cases.html`/`case-detail.html` refs remain under public/. Live mirror-server probes (forged PHPSESSID session file): anon `?id=<uuid>` -> 302 `/auth/login.php`; anon no-id -> 302 login; authed malformed id -> 302 `/admin/cases.php`. Stub-PDO harness (FakePdo extends PDO routing canned rows by SQL sniff, session-seeded admin, drove the REAL case-detail.php incl. real repositories; generated+deleted in temp): 57/57 assertions PASS - head/title/meta-desc/fonts/leaflet/importmap-chartjs; state JSON (caseId, embedded + top-level report/rescuer, activity length, photos normalization incl `{url}` form + junk-drop, error:null); page-head actions (See location/Reassign/Resolve present, NO disabled attr anywhere); timeline (5 events incl open, assigned note, status_change mapped note, unknown-action fallback 'Note Added' + escaped raw note, shield+Admin outline badge markup); info panel (6 dialog rows, stamp--accent 'In Progress', shortIds, time_ago cells); source report (title '&middot;' shortId, animal_description `<script>` payload neutralized by e(), Location/Validation/Report-status); attachments (count chip 2, exactly 2 cd-file links, alt/loading/target-rel); proof empty-state + camera icon + meta badge correctly absent; grid column classes all present.
- [2.7, 2026-08-23] DONE + verified (AUDIT-THEN-COMPLETE: a parallel session had dropped an UNVERIFIED health-records.php + health-records.js hydration patch with no ledger entry — audited line-by-line against the unit spec, fixed 5 deviations listed below, did NOT rewrite compliant work): created/verified `public/admin/health-records.php` (bootstrap mirrors cases.php: autoload+Dotenv → `$requiredRole='admin'` + guard → ui-helpers → `Database::connect()` → data builders → SSR children into `$adminChildren` via admin-shell capture + site-head with `$fontsHref` Fraunces/Nunito/IBM-Plex-Mono, `$pageCss=['/admin/css/admin.css']`, `$importMapExtras=['chart.js'=>'https://esm.sh/chart.js@4.4.4/auto']`). Data mirrors controller serialization verbatim: records SQL+mapping == `HealthController::records()` (full join query incl. latest field_status/vitals subqueries; camelCase map incl. vaccinationDetails JSON-decode, hasMedicalRecord bool, updatedAt = medical ?? animal); activity == `HealthController::activity()` (400 daily buckets + same 3 fill SQLs). PHP ports JS derivations for the default view (`filter=all, query='', sort=newest, range=30d, species=all, page=1, queueExpanded=false`) incl. BOTH JS date quirks: `daysUntil(null)`→epoch≈-20696 days (drives Overdue tab count, attention queue critical entries, nav badge) AND `new Date(x).getTime()<Date.now()` KPI overdue rule. DELETED `public/admin/health-records.html`.
  - **Deviations fixed from the dropped parallel-session work:** (1) FIDELITY BUG — its `$hrOverdueNow(null)` returned false but kpis.js computes `new Date(null).getTime() < Date.now()` → epoch → TRUE; null-due records must count into the 'Overdue checkups' KPI (its own `$hrDaysUntil` already replicated the epoch quirk correctly — internally inconsistent). (2) Export CSV button used shared `BTN_BASE` which lacks cva base groups `disabled:pointer-events-none disabled:opacity-50` → now emits final twMerge-resolved string locally (HR_BTN_BASE constant, reports.php pattern). (3) `animals-flyout.js` forward link `health-record.html?id=` → `/admin/health-record.php?id=` (2.8 owns target). (4) dead back-link in `pages/health-record/page.js` `health-records.html` → `/admin/health-records.php`. (5) hydration branch was missing the legacy post-render side-effect `setNavBadge("health", allAttentionCount())` (keeps swr sessionStorage badge fresh for still-.html pages' sidebars; 2.6 precedent).
  - **Chart handling decision:** ALL four Chart.js canvases (dog/cat donuts, trend line, barangay stacked bar) HYDRATE client-side via existing `mountCharts()` — they mutate canvas after boot; SSR renders only containers/canvas ids/donut-center totals/status legends/vax lists/toggle buttons. Hydration branch: `Object.assign(state, __PAGE_STATE__)` → empty-#app fallback re-render w/ requireAuth (guard kept) → else skip initial render entirely (no rerenderAll, no double paint): setNavBadge + createIcons/initShell/initDropdownMenu/initHealthRecordsEvents/initAnimalsFlyout/initDate/mountCharts. Legacy fetch flow intact as fallback. `app-shell.js` NAV_TARGETS `'health records' → /admin/health-records.php`.
  - **`window.__PAGE_STATE__` shape (keys = pages/health-records/state.js module keys EXACTLY, order-preserving):** `{filter:'all', query:'', sort:'newest', range:'30d', species:'all', page:1, queueExpanded:false, records:[HealthController records ×N created_at DESC], activity:[400 {date,checkups,treatments,vaccinations}]}`.
  - Class-string notes: Badge cva base+success/accent/destructive variants emitted via local `$hrBadge` (Badge does NOT escape text → e() applied); q-tabs buttons `q-btn[ is-active]` label ` &middot; N` counts over FULL dataset; table rows replicate RecordsTable exactly (hr-avatar initials ×2 upper, hr-species `cap · cap`, mono MM/DD cells, next-due stamp coral(<0)/muted(<=14)/accent, condition Healthy→success else destructive); pagination `queue-pagination#hr-pagination` wrapper only when >8 (PAGE_SIZE=8) using shared `pagination_bar`; queue cards `hr-queue-card--critical|--warn|--soon` + `stamp hr-qc-days hr-qc-days--<tier>`, title attr `name · barangay · text — open in records`, tally stamps, `View all N` when items>6, grid class WITHOUT is-expanded at defaults; KPI strip 6 tiles (dark stethoscope tile, `+N%` accent note always on Fully vaccinated, Action coral note only when overdue>0).
  - Deviations accepted: locale/TZ-dependent date rendering server-side accepted since 2.1 (fmtDate short `Mon D, YY` / mono `MM/DD`, daysUntil computed in server TZ — matches UTC+8 browsers for ISO date strings); `cap()` first-char-only quirk mirrored ('shi tzu' → 'Shi tzu'); stable sorts rely on PHP≥8 usort stability; notifications sidebar badge stays static 3 (JS page passes `notifications:3`, so NO null suppression unlike reports/rescuers); observed-but-NOT-touched: cases.php/animals.php Export CSV buttons use plain button_html (missing disabled:* groups) — pre-existing minor divergence owned by those units, locked rule forbids editing them here.
   - Verified: `php -l` clean (health-records.php + harness); `node --check` clean on health-records.js, animals-flyout.js, pages/health-record/page.js, app-shell.js; zero `health-records.html`/`health-record.html` refs remain under public/; anon `/admin/health-records.php` → 302 `/auth/login.php` (+ index.php regression-clean 302; old .html now 404). Mirror-only harness (`public/admin/harness-health-records.php`, FakePdo extends PDO routing canned rows by SQL sniff BEFORE vendor/autoload, session-seeded admin, drove REAL health-records.php; generated+deleted in temp): **101/101 assertions PASS** — head/fonts/css/importmap/module tag; shell/active-nav/badge overrides (health=10, static bell 3)/pravatar; page-head incl. full cva Export CSV string; 6 filter tabs with counts; all 8 region wrappers; KPIs 10/4(+40%)/3/6(Action)/6(dark×1)/105 incl. null→epoch overdue; donuts 5/5 + legends + vax lists; top conditions stable order slice-6 widths 100%/25% Parvovirus dropped; trend/stacked containers + toggle is-active; queue epoch-days label (~20696d overdue critical), day-sorted cards, Spike×2 cards Kitty cut at 7th, tally 5/5, View all 10; table page-1 newest-first 8 rows + pagination aria-current, badge/stamp variants, em-dash null dates, XSS payloads escaped in DOM (`Mar<vi>n "&x"` neutralized, O'Brien → &#039;) while raw in JSON state; activity 400 buckets today/yesterday tallies; no password_hash/loading states anywhere.
- [2.8, 2026-08-23] DONE + verified: created `public/admin/health-record.php` (full SSR drill-down for single animal, bootstrap autoload+Dotenv → `$requiredRole='admin'` + guard → ui-helpers → UUID validation of `$_GET['id']` BEFORE `Database::connect()` → Repositories mirroring `HealthController::record()` verbatim → SSR children into `$adminChildren` via `admin-shell.php` capture + `site-head.php` with `$fontsHref` Fraunces/Nunito/IBM-Plex-Mono, `$pageCss=['/admin/css/admin.css']`). Data builders mirror controller serialization exactly: animal = `Repository('animals')->find($id)`; health status = latest `animal_field_status`; medical = `Repository('animal_medical_records')->findBy('animal_id',$id)`; vitals = `Repository('vitals_log')->all(...)`; documents = `Repository('animal_documents')->all(...)`; vaccination mapping = port of `HealthController::record()` vaccination_records fallback logic; reminders = controller's `$addReminder` closure port; history = controller's usort history build; protocols = `VaccinationEngine::protocolsForSpecies()`. DELETED `public/admin/health-record.html`. PATCHED `public/admin/js/health-record.js`: hydration branch (`window.__PAGE_STATE__` → `_record = state.record`, server-painted guard via `app.childElementCount`, then createIcons/initShell/initDropdownMenu/initHealthRecordEvents; legacy fetch flow kept verbatim as fallback); back-link in `page.js` `PageHead()` already `/admin/health-records.php`. PATCHED `public/admin/js/pages/health-record/page.js`: extracted event wiring into exported `initHealthRecordEvents()` called by both `renderHealthRecord()` and hydration branch.
- **`window.__PAGE_STATE__` shape (keys = page.js `_record` shape + wrapper):** `{record:{id,hasMedicalRecord,name,species,breedType,sex,ageEstimate,birthDate,barangay,adoptionStatus,photoUrl,overview{healthStatus,vaccinationStatus,deworming,neutered,notes,notesMeta},history[],vaccinations[],reminders[],vitals[],vitalMeta,heartRateHistory[],documents[],protocols[],ageWeeks:null}}`.
- **Class-string notes:** page-head back-link `/admin/health-records.php` + Edit/Add Health Record actions; profile panel `hr-profile-panel` with photo/initial + `hr-detail-row` list; overview `hr-subcards` 4-up with tint-circle + pill chip + interpretation notes; history `hr-timeline` with dot/content/date/doctor; vaccination table `table-wrap > table` with status pills; reminders `hr-reminder-list` with tint-circle + pill days; vitals `hr-vital-list` + meta; documents `hr-doc-table-wrap` table slice(0,3); stats `hr-stat-strip` 4-up; all icons via `data-lucide` hydrated by createIcons.
- **Deviations accepted:** `ageWeeks` hardcoded `null` (controller returns null today; engine computes from birth date client-side); profile rows filter out null/empty values server-side matching JS `.filter((row) => row[2] != null && row[2] !== "")`; `vaccinationDetails` legacy fallback mapping ports controller's `dateGiven`/`nextDue`/`doseNumber`/manufacturer fields verbatim; `hasMedicalRecord` uses `!empty($medical)` matching controller `!empty($medical)`; `$navBadges=['notifications'=>3]` (static bell badge, health records page never overrides).
- **Verified:** `php -l` clean; `node --check` clean on health-record.js + page.js; zero `health-record.html` refs remain under public/; anon `/admin/health-record.php?id=<uuid>` → 302 `/auth/login.php`; malformed id → 302 `/admin/health-records.php`; unknown id → 302 `/admin/health-records.php`. Mirror harness (FakePdo extends PDO, session-seeded admin, canned animal/medical/vitals/documents rows, drove REAL health-record.php, generated+deleted in temp): **47/47 assertions PASS** — head/title/fonts/css; shell/active-nav "health records"/pravatar; page-head back-link + Edit/Add buttons; profile panel (photo/initial + 6 detail rows); overview 4 subcards + interpretation notes; history timeline 3 items; vaccination table 2 rows + status pills; reminders 2 items; vitals 2 items + meta; documents table 2 rows; stats 4 tiles; valid `__PAGE_STATE__` JSON with record payload; XSS payloads escaped in DOM; no password_hash leaks.

## Phase 2 complete — USER SMOKE-TEST CHECKLIST

Once you add your own `.env` with real MySQL80 credentials:

1. Start the server: `php -S 127.0.0.1:8000 -t public public\index.php`
2. Login at `/auth/login.php` with `admin@furescue.local` / `Password123!`
3. Visit every migrated admin route and confirm rows render identically to pre-migration screenshots/git history:
   - `/admin/index.php` (dashboard)
   - `/admin/cases.php` (cases list)
   - `/admin/case-detail.php?id=<case-id>` (case detail drill-down)
   - `/admin/animals.php` (animals list)
   - `/admin/reports.php` (reports list)
   - `/admin/rescuers.php` (rescuers list)
   - `/admin/health-records.php` (health records list)
   - `/admin/health-record.php?id=<animal-id>` (health record detail drill-down)
4. Repeat step 2–3 under a **rescuer** account to prove role guards (rescuer sees only permitted pages, admin sees all).

If any page shows a blank shell or 302 loop, check `.env` DB creds + `guard.php` role mapping first.

## Cross-cutting facts every unit relies on
- Portable PHP CLI: `%TEMP%\opencode\furescue-php\php.exe` (8.3.33; pdo_mysql/openssl/mbstring/zip on; **no pdo_pgsql**).
- Verification mirror: `%TEMP%\opencode\furescue-mirror` (repo copy WITH vendor/). Repo root has NO vendor and OneDrive blocks composer there — never composer-install in the repo.
- Test loop: copy changed files repo→mirror (same relative paths), start DETACHED server (`Start-Process`, NOT Start-Job — jobs die with the shell), curl assertions, kill process when done.
- NO working DB credentials exist. MySQL80 runs locally but documented demo creds are access-denied; user wires `.env` themselves later. Therefore: verify HTTP 200 + shell/guard DOM markers + `php -l`; DB-row fidelity is assured by code review against controller serialization, not live data. DB-backed smoke test instructions ship in the final handoff for the user.
- `public/index.php` flow: `'/'` → homepage branch; real existing files under `public/` → native serve/exec via `return false` passthrough; everything else → API router. New `public/admin/*.php` pages execute directly once created.
- `guard.php` contract: define `$requiredRole` BEFORE `require __DIR__ . '/../includes/guard.php';`. Anon → 302 `/auth/login.php`. Wrong role → 302 `/index.php`.
