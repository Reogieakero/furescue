# UNIT 2.4 — Animals: `public/admin/animals.html` → `public/admin/animals.php`

You are executing one unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. Unit 2.0 must be ✅ (shared `public/includes/admin-shell.php` exists — use its contract from the ledger). Units 2.1–2.3 recommended as pattern references.
2. On completion update HANDOFF.md: row 2.4 ✅ + dated ledger entry.

## Global locked rules
- Full PHP SSR; pages call Entities/Repositories/Services in-process — NO fetch for initial render. NEVER edit `src/Controllers|Repositories|Services|Entity|Http`, migrations, tests, seeders, dbtool, bin. No frameworks. No new composer deps. PHP ≥ 8.1.
- EVERY echoed dynamic value through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`. Pixel-identical DOM/classes to current JS output; never rename classes.
- Auth via sessions; set `$requiredRole` from the entry JS `requireAuth([...])` call, then `require __DIR__ . '/../includes/guard.php';`.
- Environment: portable PHP `%TEMP%\opencode\furescue-php\php.exe`; mirror `%TEMP%\opencode\furescue-mirror` (copy changed files before testing); DETACHED server via `Start-Process -ArgumentList "-S","127.0.0.1:<port>","-t","...public","...index.php"` (never Start-Job); NO DB creds exist — verify 200 + DOM markers + `php -l`, data fidelity by code review against controller serialization; harnesses only in mirror.
- Class strings: emit FINAL tailwind-merge-resolved strings. Read every component `.js` before writing its PHP twin.

## Locked SSR architecture
Same pattern as prior units: PHP builds payloads via Repositories mirroring CONTROLLER serialization exactly → renders FULL page identical to post-load JS DOM → embeds `<script>window.__PAGE_STATE__ = <?= json_encode($state, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT) ?>;</script>` → entry JS gets minimal hydration branch (merge embedded state, SKIP initial render + client requireAuth, keep ALL interactivity: search/filter inputs, card modals, add-animal flow, image handling); legacy fetch flow kept as fallback. Server always paints full page, never skeletons.

## Work items
1. READ FIRST: `public/admin/js/animals.js`, everything under `public/admin/js/pages/animals/` (state + components), old `public/admin/animals.html`. Identify which fetches build initial view (animals list endpoint → its controller method's exact serialization; likely also a `/health/records` call that yields the has-medical-record flag set used on cards — trace it) and what stays pure interactivity.
2. Write `public/admin/animals.php`: bootstrap autoload+Dotenv, guard, data builders (animal rows + medical-record flag map computed server-side exactly as JS does), full SSR of KPIs/filters bar/grid cards page 1 (+ pagination markup if the page paginates client-side — render page 1 only), `__PAGE_STATE__` embed, same script/style tags as old HTML (incl. any lightbox/gallery assets).
3. Patch `animals.js` hydration branch per architecture above. Update `app-shell.js` NAV_TARGETS `animals → /admin/animals.php`.
4. Copy to mirror; `php -l`; start server; curl anon → expect 302 `/auth/login.php`; harness-authenticated render check if needed.
5. Delete `public/admin/animals.html` ONLY after verification passes.

## Done when
PHP lints; anon redirect proven; grid/KPI markup path verified; hydration avoids double-render; HANDOFF.md updated (state shape + class-string notes for animal cards/health ribbons).

## Report format
Status | files created/modified/deleted | state shape summary | tricky class-string computations | deviations/blockers.
