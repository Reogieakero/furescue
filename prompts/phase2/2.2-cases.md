# UNIT 2.2 — Cases: `public/admin/cases.html` → `public/admin/cases.php`

You are executing one unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. Unit 2.0 must be ✅ (shared `public/includes/admin-shell.php` exists — use its parameter contract from the ledger). Unit 2.1 recommended (pattern reference) but only 2.0 is required.
2. On completion update HANDOFF.md: row 2.2 ✅ + dated ledger entry.

## Global locked rules
- Full PHP SSR; pages call Entities/Repositories/Services in-process — NO fetch for initial render. NEVER edit `src/Controllers|Repositories|Services|Entity|Http`, migrations, tests, seeders, dbtool, bin. No frameworks. No new composer deps. PHP ≥ 8.1.
- EVERY echoed dynamic value through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`. Pixel-identical DOM/classes to current JS output; never rename classes.
- Auth via sessions; set `$requiredRole` from the page's own entry JS `requireAuth([...])` call (likely `'admin'`; use exactly what cases.js passes), then `require __DIR__ . '/../includes/guard.php';`.
- Environment: portable PHP `%TEMP%\opencode\furescue-php\php.exe`; mirror `%TEMP%\opencode\furescue-mirror` (copy changed files there before testing); DETACHED server via `Start-Process -ArgumentList "-S","127.0.0.1:<port>","-t","...public","...index.php"` (never Start-Job); NO DB creds exist — verify 200 + DOM markers + `php -l`, data fidelity by code review against controller serialization; harnesses only in mirror.
- Class strings: emit FINAL tailwind-merge-resolved strings (later same-group utility wins over earlier base). Read every component `.js` before writing its PHP twin.

## Locked SSR architecture
Same pattern as dashboard unit: PHP builds payloads via Repositories mirroring CONTROLLER serialization exactly (JS renders off API field names; entity `toArray()` may differ) → renders FULL page identical to post-load JS DOM → embeds `<script>window.__PAGE_STATE__ = <?= json_encode($state, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT) ?>;</script>` before module script → entry JS (`public/admin/js/cases.js`) gets a minimal hydration branch: if `window.__PAGE_STATE__` exists merge into state, SKIP initial render + client requireAuth redirect, still run all init fns (icons, shell, dropdowns, filters, modals/workflow actions, map if any); legacy fetch flow kept as fallback. Server always paints full page, never skeletons/loading states.

## Work items
1. READ FIRST (do not write code until done): `public/admin/js/cases.js`, everything under `public/admin/js/pages/cases/` (state.js and all components — kpi/list/map/util/workflow/page as they exist), old `public/admin/cases.html` (head fonts/scripts, leaflet tags if used). Identify: which fetches build initial view, the exact state keys components read, which parts are pure interactivity (stay in JS).
2. Trace data source: the cases list endpoint → find its controller method (CaseController::index or similar) and note EXACT serialization incl. the `assigned_rescuer_name` join origin; replicate query+shape via CaseRepository in-process. KPI cards derive counts client-side from the fetched rows — compute identically in PHP over the same row set.
3. Write `public/admin/cases.php`: bootstrap autoload+Dotenv, guard, data builders, KPI cards + table rows (+ map container markup if present) rendered server-side with identical classes, `__PAGE_STATE__` embed, then the SAME script/style tags as the old HTML head/body.
4. Patch `cases.js` hydration branch per architecture above. Update `app-shell.js` NAV_TARGETS `cases → /admin/cases.php`. Any in-page links to other UNCONVERTED pages stay `.html` (their units flip them later).
5. `workflow.js` navigates to `case-detail.html?id=...` — LEAVE AS-IS this unit; unit 2.3 flips it. Note it in HANDOFF ledger so 2.3 knows.
6. Copy to mirror; `php -l`; start server; curl anon → expect 302 `/auth/login.php`. Mirror-only harness for authenticated render check if needed.
7. Delete `public/admin/cases.html` ONLY after verification passes.

## Done when
cases.php lints + serves 200 with expected markers (KPI section, table header, at least skeleton-free row markup path), anon redirect proven, cases.js hydrates from embedded state without double-render, HANDOFF.md updated (incl. state-shape contract + class-string notes for table rows/status badges).

## Report format
Status | files created/modified/deleted | state shape summary | tricky class-string computations | deviations/blockers.
