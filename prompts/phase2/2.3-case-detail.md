# UNIT 2.3 — Case-detail: `public/admin/case-detail.html` → `public/admin/case-detail.php`

You are executing one unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. Unit 2.2 must be ✅ (cases.php exists — this page is its drill-down). Use the admin-shell contract + state-shape notes from the ledger.
2. On completion update HANDOFF.md: row 2.3 ✅ + dated ledger entry.

## Global locked rules
- Full PHP SSR; pages call Entities/Repositories/Services in-process — NO fetch for initial render. NEVER edit `src/Controllers|Repositories|Services|Entity|Http`, migrations, tests, seeders, dbtool, bin. No frameworks. No new composer deps. PHP ≥ 8.1.
- EVERY echoed dynamic value through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`. Pixel-identical DOM/classes to current JS output; never rename classes.
- Auth via sessions; set `$requiredRole` from the entry JS `requireAuth([...])` call, then `require __DIR__ . '/../includes/guard.php';`.
- Environment: portable PHP `%TEMP%\opencode\furescue-php\php.exe`; mirror `%TEMP%\opencode\furescue-mirror` (copy changed files there before testing); DETACHED server via `Start-Process -ArgumentList "-S","127.0.0.1:<port>","-t","...public","...index.php"` (never Start-Job); NO DB creds exist — verify 200/redirects + DOM markers + `php -l`, data fidelity by code review against controller serialization; harnesses only in mirror.
- Class strings: emit FINAL tailwind-merge-resolved strings. Read every component `.js` before writing its PHP twin.

## Locked SSR architecture
Same pattern as prior units: PHP builds the case payload (+ activity timeline payload) via Repositories mirroring CONTROLLER serialization exactly → renders FULL page identical to post-load JS DOM for the loaded case → embeds `<script>window.__PAGE_STATE__ = <?= json_encode($state, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT) ?>;</script>` → entry JS (`public/admin/js/case-detail.js`) gets a minimal hydration branch: merge embedded state, SKIP initial render + client requireAuth, keep ALL interactivity (tabs, status workflow actions, note forms, modals, media gallery); legacy fetch flow kept as fallback when no `__PAGE_STATE__`.

## Work items
1. READ FIRST: `public/admin/js/case-detail.js`, everything under `public/admin/js/pages/case-detail/` (state + components incl. page.js), old `public/admin/case-detail.html` head/scripts. Identify how the JS reads the id (`?id=` query param), which fetches build initial view (case detail endpoint + activity/timeline endpoint — find BOTH controller methods and their exact serialization), and what stays pure interactivity.
2. Query-param handling in PHP: read `$_GET['id']`; validate as UUID shape (regex). Missing/malformed → `header('Location: /admin/cases.php'); exit;`. Not-found after repository lookup → same redirect (matches SPA behavior of bouncing bad ids; confirm what the JS actually does on 404 and mirror THAT instead if different).
3. Write `public/admin/case-detail.php`: bootstrap autoload+Dotenv, guard, id validation, data builders, full server-side render (case header/status block, timeline entries page 1 only if paginated, tabs markup with active tab matching JS default), `__PAGE_STATE__` embed, same script/style tags as old HTML.
4. Patch `case-detail.js` hydration branch per architecture above.
5. Link flips NOW allowed: any `'cases.html'` back-links inside case-detail page JS/components → `/admin/cases.php`; AND flip the forward link left by unit 2.2 in `pages/cases/workflow.js` (`case-detail.html?id=` → `/admin/case-detail.php?id=`). Update `app-shell.js` NAV_TARGETS only if a sidebar key points at this page (likely not — it's row-click navigation).
6. Copy to mirror; `php -l`; start server; curl anon no-id → expect 302 to login (guard fires before id logic) ; harness-authenticated no-id → expect 302 cases.php; with-id path verified via code review (no DB).
7. Delete `public/admin/case-detail.html` ONLY after verification passes.

## Done when
PHP lints; guard + bad-id redirects proven; hydration branch avoids double-render; both link directions flipped (.html references gone from touched files except other units' pages); HANDOFF.md updated.

## Report format
Status | files created/modified/deleted | state shape summary | redirect/validation decisions | class-string notes | deviations/blockers.
