# UNIT 2.6 — Rescuers: `public/admin/rescuers.html` → `public/admin/rescuers.php`

You are executing one unit of a static-HTML-SPA → server-rendered-PHP migration for **FurEscue**, repo root `C:\Users\nicol\OneDrive\Documents\reagan\furescue` (Windows, PowerShell 5.1).

## STEP 0 — Handoff protocol (mandatory)
1. Read `prompts/phase2/00-HANDOFF.md`. Unit 2.0 must be ✅ (shared admin-shell include). Units 2.1–2.5 recommended as pattern references.
2. On completion update HANDOFF.md: row 2.6 ✅ + dated ledger entry.

## Global locked rules
- Full PHP SSR; pages call Entities/Repositories/Services in-process — NO fetch for initial render. NEVER edit `src/Controllers|Repositories|Services|Entity|Http`, migrations, tests, seeders, dbtool, bin. No frameworks. No new composer deps. PHP ≥ 8.1.
- EVERY echoed dynamic value through `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`. Pixel-identical DOM/classes to current JS output; never rename classes.
- Auth via sessions; set `$requiredRole` from the entry JS `requireAuth([...])` call, then `require __DIR__ . '/../includes/guard.php';`.
- Environment: portable PHP `%TEMP%\opencode\furescue-php\php.exe`; mirror `%TEMP%\opencode\furescue-mirror` (copy changed files before testing); DETACHED server via `Start-Process -ArgumentList "-S","127.0.0.1:<port>","-t","...public","...index.php"` (never Start-Job); NO DB creds exist — verify 200 + DOM markers + `php -l`, data fidelity by code review against controller serialization; harnesses only in mirror.
- Class strings: emit FINAL tailwind-merge-resolved strings. Read every component `.js` before writing its PHP twin.

## Locked SSR architecture
Same pattern as prior units: PHP builds payloads via Repositories mirroring CONTROLLER serialization exactly → renders FULL page identical to post-load JS DOM → embeds `<script>window.__PAGE_STATE__ = <?= json_encode($state, JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT) ?>;</script>` → entry JS gets minimal hydration branch (merge embedded state, SKIP initial render + client requireAuth, keep ALL interactivity: approve/reject pending rescuers, duty-status toggles, modals); legacy fetch flow kept as fallback. Server always paints full page, never skeletons/loading states.

## Work items
1. READ FIRST: `public/admin/js/rescuers.js`, everything under `public/admin/js/pages/rescuers/` (state + components), old `public/admin/rescuers.html`. Identify which fetches build the split view (pending applications list + active roster; likely two `/users?role=rescuer&account_status=...` calls plus duty status), and what stays pure interactivity.
2. Trace data source: UserController list endpoint's EXACT serialization; replicate both queries via UserRepository in-process. Counts/badges derive from row sets — compute identically.
3. Write `public/admin/rescuers.php`: bootstrap autoload+Dotenv, guard, data builders, full SSR of split view (pending column + active roster cards incl. avatars/initials logic replicated from JS helpers), `__PAGE_STATE__` embed, same script/style tags as old HTML.
4. Patch `rescuers.js` hydration branch per architecture above. Update `app-shell.js` NAV_TARGETS `rescuers → /admin/rescuers.php`.
5. Copy to mirror; `php -l`; start server; curl anon → expect 302 `/auth/login.php`; harness-authenticated render check if needed.
6. Delete `public/admin/rescuers.html` ONLY after verification passes.

## Done when
PHP lints; anon redirect proven; split-view markup path verified; hydration avoids double-render; HANDOFF.md updated (state shape + class-string notes for roster cards/status pills).

## Report format
Status | files created/modified/deleted | state shape summary | tricky class-string computations | deviations/blockers.
